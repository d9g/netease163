# 核心
pyncm>=1.8.0          # 网易云 API 封装 (含加密)
requests>=2.28.0      # 备用 HTTP
typer>=0.9.0          # CLI 框架 (替换 argparse)

# 数据库
sqlalchemy>=2.0.0     # ORM
PyMySQL>=1.0.0        # MySQL 驱动 (可选)

# API 服务
fastapi>=0.104.0      # Web 框架
uvicorn[standard]>=0.24.0  # ASGI 服务器
pydantic>=2.0.0       # 数据校验

# 工具
python-dotenv>=1.0.0  # .env 加载
loguru>=0.7.0         # 日志
beautifulsoup4>=4.12.0  # HTML 解析 (lyric / search 爬虫)

# 调度 + 异步
APScheduler>=3.10.0   # 定时任务 (scheduler.py + scripts/auto_scheduler.py)

# LLM / AI 分析
httpx>=0.25.0         # LLM 调用 (comment_analyzer.py 用)

# 登录 / 二维码
qrcode>=7.4.0         # 扫码登录 (login.py)
pillow>=10.0.0        # 二维码图片生成

# 加密 (登录态加密存储)
pycryptodome>=3.19.0

# 加密 (登录态加密存储, P1-7 修复: 之前缺 cryptography 会静默降级明文)
cryptography>=42.0.0

# 测试 (P1-7 修复: 之前测试跑不起来)
pytest>=7.4.0
pytest-asyncio>=0.23.0
httpx>=0.25.0  # FastAPI TestClient 需要
