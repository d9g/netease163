# netease163 WebUI 全面升级计划

老杨 21:41 提出 10 个优化点，分阶段实施。

## 阶段 1：后端按名称搜索（最关键）

### 1.1 新增按名称搜索 endpoint（统一接口）

每个 spider 加 `search()` 方法，复用 `SearchSpider`：
- `LyricSpider.search(name)` - 按歌名搜，返回候选列表 + 各 ID
- `CommentSpider.search(name)` - 同上
- `SongSpider.search(name)` - 同上
- `PlaylistSpider.search(name)` - 歌单名称
- `ArtistSpider.search(name)` - 歌手名称
- `AlbumSpider.search(name)` - 专辑名称
- `RadioSpider.search(name)` - 电台（看 pyncm 是否支持）
- `ToplistSpider` 不需要搜索（榜单固定）

### 1.2 重名二次筛选接口

- `GET /api/v1/search/candidates?type=song&q=七里香` → 返回 5-10 个候选
- 候选包含：id / name / artists / album / duration
- 前端展示列表 + 点击进入 ID 详情

## 阶段 2：前端一体化设计

### 2.1 通用 helper 函数

```js
async function searchByName(type, keyword) { ... }  // 返回候选列表
async function loadDetail(type, id) { ... }         // 详情
function renderCandidates(type, list) { ... }       // 列表渲染
```

### 2.2 各页面改造

| 页面 | 旧 | 新 |
|---|---|---|
| 搜索 | 只返回清单 | 歌曲卡片，点击进歌曲详情 + 评论 |
| 歌词 | 只按 ID | 按歌名搜 → 候选 → 选 ID → 歌词 |
| 评论 | 只按 ID | 按歌名搜 → 候选（显示评论数）→ 选 ID → 评论 |
| 歌曲详情 | 只按 ID | 按歌名搜 → 候选 → 选 ID → 详情 |
| 歌单 | 只按 ID | 按名称搜 → 候选 → 选 ID → 歌单内容 |
| 歌手 | 只按 ID | 按名称搜 → 候选 → 选 ID → 歌手资料 |
| 专辑 | 只按 ID | 按名称搜 → 候选 → 选 ID → 专辑曲目 |
| 电台 | 只按 ID | 按名称搜（看支持）→ 候选 → 选 ID → 电台节目 |
| 排行榜 | 列表 | 点击歌曲 → 评论（智能：有缓存直接展示 + 后台异步更新） |

### 2.3 我的音乐

- 红心歌单 ✅ (已有)
- 我的歌单列表 ✅ (已有)
- 我的收藏（专辑）
- 每日签到

## 阶段 3：评论智能缓存

- /api/v1/comment/{song_id} - 检查 DB 有无评论
  - 有 → 直接返回
  - 无 → 调 CommentSpider.fetch → 写库 → 返回
- 后台 scheduler 每 6h 同步 Top 50 热门评论（更新评论数）

## 执行顺序

- [ ] 阶段 1: 后端 7 个 search() 方法 + 1 个 candidates endpoint
- [ ] 阶段 2: 前端 9 页面全部改造
- [ ] 阶段 3: 评论缓存逻辑
- [ ] 端到端验证 + commit