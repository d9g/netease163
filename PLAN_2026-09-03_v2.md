# netease163 WebUI v2 修复计划 (老杨 23:35)

老杨 23:35 提出 9 个问题, 已确认按方案 A-G 修. 阶段 1 后端, 阶段 2 前端.

## 阶段 1: 后端修复 (P0)

### 1.1 修 my_playlists NoneType bug (问题 9)
- [ ] `netease163/login/login.py:237` 改 `p.get("creator") or {}` 链式保护
- [ ] uid=0 时优雅降级返回 `playlists=[]`

### 1.2 后端 search 接口支持 comment_total (问题 1/2)
- [ ] `/api/v1/candidates?q=&type=song` 加 comment_total (前 8 候选)
- [ ] `/api/v1/comment/by-name?q=` 已经有, 验证

### 1.3 排行榜榜单 ID 修复 (问题 7)
- [ ] pyncm 1.8.1 没榜单 API, 改用 playlist ID 模拟
- [ ] 内置 4 大官方榜: 热歌 3779629 / 飙升 19723756 / 新歌 3778678 / 原创 2884035
- [ ] 前端 /toplist 按钮组 → 调 /playlist/{id} 走 playlist 详情路径

### 1.4 关键词管理端点 (问题 8)
- [ ] `GET /api/v1/keywords` 列表 (带分页)
- [ ] `POST /api/v1/keywords` 手动添加 (敏感词过滤)
- [ ] `DELETE /api/v1/keywords/{id}` 删除
- [ ] 内置敏感词库 (~100 个): 涉政/违禁/平台/低俗

### 1.5 触发抓取评论端点 (问题 1)
- [ ] `POST /api/v1/crawl/comment/{song_id}` 立即抓取评论
- [ ] DB 无评论时自动触发 (后端异步)

## 阶段 2: 前端 UI 改造 (P0)

### 2.1 搜索清单加评论数 + 点击触发抓取 (问题 1)
- [ ] `doSearch()` 渲染 cards 加 `💬 N 评论` badge
- [ ] `showSongWithComments(song_id)` 若 DB 0 评论 → 弹 toast "评论抓取中" + 后端异步入队

### 2.2 歌词清单加评论数 (问题 2)
- [ ] `lyricSearch()` candidates 加 comment_total

### 2.3 歌曲详情优化 (问题 3)
- [ ] album 显示 cover_url 图片 + 替换 [object Object]
- [ ] 跳转歌词/评论: `showPage('lyric'); prefillSongId(id); auto-fetch` 直达
- [ ] `loadLyricById` / `loadCommentById` 接收 song_id 直传参数

### 2.4 歌单曲目加评论数 (问题 4)
- [ ] `loadPlaylistById` tracks 加 comment_total
- [ ] 曲目卡片显示 `💬 N 评论`

### 2.5 专辑曲目修复 (问题 5)
- [ ] 前端用 `data.tracks` 不是 `data.songs`
- [ ] 改完应展示 10 首歌 (范特西)

### 2.6 电台菜单改造 (问题 6)
- [ ] 隐藏电台菜单 OR 加 "暂不可用" 说明
- [ ] 改成 ID 输入 + 提示

### 2.7 排行榜 UI 改造 (问题 7)
- [ ] `loadToplists()` 用内置 4 大榜单 ID 列表
- [ ] 调 /playlist/{id} 复用歌单渲染

### 2.8 随机爬取扩词 + 统计 (问题 8)
- [ ] 手动扩词: 输入框 + 敏感词过滤提示 + add 按钮
- [ ] 关键词池: 列表 + 删除按钮
- [ ] 统计页: 总入库歌曲加链接 → 展示入库清单

### 2.9 我的音乐修报"需要登录" (问题 9)
- [ ] 后端 my_playlists 修 NoneType → 前端不再报错

## 阶段 3: 验证

- [ ] 12 个菜单端到端测试
- [ ] 浏览器实测 chromium dump
- [ ] git commit + restart service

## 关键决策

- A: 实时拉评论数 (30s 内返回) - 跟 by-name 一致
- B: 无评论自动入队 - 抓取后写 DB, 下次直接有
- C: 专辑 `tracks` 字段 - 改前端
- D: 电台隐藏菜单 - pyncm 不支持
- E: playlist ID 模拟榜单 - 4 大官方榜
- F: 内置敏感词库 (~100 个) - 政治/违禁/平台/低俗
- G: my_playlists NoneType 链式保护 - 已找到